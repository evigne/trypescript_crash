// Basic Data Types

let myString: string;
let myNumber: number;
let myAge: any; // booleab,null,etc..
myString = "Hello World";
myNumber = 1;
myAge = "twenty seven";

// Array

let strArray: string[];
let strList: Array<string> = ["Vignesh", "Vicky"];
let numArray: number[]; // boolean[]
let numList: Array<number> = [1, 2, 3]; // Another method to declare array

strArray = ["hello", "world"];
numArray = [1, 2, 3, 4, 5];

// Tuple Array with defined number of elements

let strNumTuple: [string, number];
strNumTuple = ["Vignesh", 5];

console.log(numArray);

//

let myVoid: void = undefined;
let myNull: null = null;
let myUndef: undefined = undefined;
